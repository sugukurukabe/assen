node_modules/
dist/
.env
*.log
.DS_Store
coverage/
src/apps/approval-ui/dist/
src/apps/approval-ui/node_modules/

# ローカルのバックアップ・復元ドリル用ダンプ（本番の秘密情報を含む可能性があるためcommitしない）
# Local backup/restore-drill dumps (may contain production-like secrets; never commit)
backups/

# cookbook-scout local candidates
.cookbook-candidates/

# Zキャリア求人抽出のローカル作業用（社外APIの取得データを公開リポジトリに載せない）
# Local workspace for Z-Career job extraction (third-party API data must not enter the public repo)
# Ruang kerja lokal untuk ekstraksi lowongan Z-Career (data API pihak ketiga tidak boleh masuk repo publik)
.zcareer/
